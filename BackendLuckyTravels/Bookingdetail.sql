CREATE TABLE IF NOT EXISTS bookingdetail (
    name VARCHAR(100),
    email VARCHAR(100),
    phone VARCHAR(15),
    stays VARCHAR(25),
    requirements VARCHAR(255),
    arrivaldate DATE,
    departuredate DATE
);